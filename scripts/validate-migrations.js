const fs=require('fs');const path=require('path');const dir=path.join(__dirname,'../src/database/migrations');const files=fs.readdirSync(dir).filter(f=>f.endsWith('.ts')).sort();const seen=new Set();for(const file of files){const stamp=file.split('-')[0];if(seen.has(stamp))throw new Error(`Duplicate migration timestamp: ${stamp}`);seen.add(stamp)}console.log(`Validated ${files.length} uniquely ordered migrations`);
