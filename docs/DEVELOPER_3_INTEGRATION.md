# Developer 3 integration

Reuse shared permission, event, audit, notification, usage, storage, realtime and internal-token services. Build storage keys through `buildWorkspaceObjectPath`. Canvas rooms use `canvas:{workspaceId}` and require active workspace membership.
