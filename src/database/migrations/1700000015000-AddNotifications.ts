import {MigrationInterface,QueryRunner}from'typeorm'; export class AddNotifications1700000015000 implements MigrationInterface{name='AddNotifications1700000015000';async up(q:QueryRunner){await q.query(`CREATE TYPE notification_type_enum AS ENUM ('workspace_invitation','invitation_accepted','member_added','member_removed','member_suspended','ai_task_completed','ai_task_failed','research_completed','approval_requested','concept_ready','export_ready','export_failed')`);await q.query(`CREATE TABLE notification(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),organization_id uuid NOT NULL REFERENCES organization(id),workspace_id uuid REFERENCES workspace(id),user_id uuid NOT NULL REFERENCES "user"(id),type notification_type_enum NOT NULL,title text NOT NULL,body text NOT NULL,metadata jsonb NOT NULL DEFAULT '{}',read_at timestamptz,created_at timestamptz NOT NULL DEFAULT now())`);await q.query(`CREATE INDEX idx_notification_user_unread ON notification(user_id,created_at DESC) WHERE read_at IS NULL`)}async down(q:QueryRunner){await q.query(`DROP TABLE notification`);await q.query(`DROP TYPE notification_type_enum`)}}
