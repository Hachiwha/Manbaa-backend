import{MigrationInterface,QueryRunner}from'typeorm';export class AddUsage1700000016000 implements MigrationInterface{name='AddUsage1700000016000';async up(q:QueryRunner){await q.query(`CREATE TYPE usage_dimension_enum AS ENUM ('workspace_count','member_count','storage_bytes','ai_input_tokens','ai_output_tokens','ai_cost_usd','research_pages','image_generations','export_bytes')`);await q.query(`CREATE TYPE usage_reservation_status_enum AS ENUM ('reserved','committed','released','expired')`);await q.query(`CREATE TABLE organization_usage(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),organization_id uuid NOT NULL UNIQUE REFERENCES organization(id),totals jsonb NOT NULL DEFAULT '{}',updated_at timestamptz NOT NULL DEFAULT now())`);await q.query(`CREATE TABLE workspace_usage(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),organization_id uuid NOT NULL REFERENCES organization(id),workspace_id uuid NOT NULL UNIQUE REFERENCES workspace(id),totals jsonb NOT NULL DEFAULT '{}',updated_at timestamptz NOT NULL DEFAULT now())`);await q.query(`CREATE TABLE usage_reservation(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),organization_id uuid NOT NULL REFERENCES organization(id),workspace_id uuid NOT NULL REFERENCES workspace(id),dimension usage_dimension_enum NOT NULL,amount numeric NOT NULL,actual_amount numeric,idempotency_key varchar(160) NOT NULL UNIQUE,status usage_reservation_status_enum NOT NULL,expires_at timestamptz NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())`);await q.query(`CREATE INDEX idx_usage_reservation_active ON usage_reservation(workspace_id,dimension,status,expires_at)`)}async down(q:QueryRunner){await q.query(`DROP TABLE usage_reservation,workspace_usage,organization_usage`);await q.query(`DROP TYPE usage_reservation_status_enum`);await q.query(`DROP TYPE usage_dimension_enum`)}}
