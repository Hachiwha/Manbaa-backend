import{BadRequestException,ForbiddenException}from'@nestjs/common';import{WorkspaceStorageService}from'./workspace-storage.service';describe('WorkspaceStorageService path security',()=>{const config={getOrThrow:(k:string)=>k.endsWith('port')?9000:k.endsWith('useSsl')?false:'x'};const service=new WorkspaceStorageService(config as never);const ctx={organizationId:'org',workspaceId:'ws'};it('builds canonical paths',()=>expect(service.buildWorkspaceObjectPath(ctx,'asset','id',1,'file.png')).toBe('org/ws/asset/id/1/file.png'));it.each(['../x','%2e%2e/x','org/other/a/i/1/x','/org/ws/a/i/1/x'])('rejects traversal or cross-workspace key %s',(key)=>{const fn=()=>service.validateWorkspaceObjectPath(ctx,'workspace-assets',key);if(key.includes('other'))expect(fn).toThrow(ForbiddenException);else expect(fn).toThrow(BadRequestException)})});
