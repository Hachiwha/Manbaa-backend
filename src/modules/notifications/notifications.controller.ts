import { Controller,Get,Param,Post } from '@nestjs/common'; import { CurrentUser } from '../../core/decorators/current-user.decorator'; import { NotificationsService } from './notifications.service';
@Controller('v1/notifications') export class NotificationsController {constructor(private readonly service:NotificationsService){} @Get() list(@CurrentUser()u:{id:string}){return this.service.list(u.id)} @Post(':notificationId/read') read(@Param('notificationId')id:string,@CurrentUser()u:{id:string}){return this.service.read(id,u.id)} @Post('read-all') readAll(@CurrentUser()u:{id:string}){return this.service.readAll(u.id)} }
