import{CanActivate,ExecutionContext,Injectable,UnauthorizedException}from'@nestjs/common';import{InternalServiceTokenService}from'./internal-service-token.service';@Injectable()export class InternalServiceGuard implements CanActivate{constructor(private readonly tokens:InternalServiceTokenService){}async canActivate(ctx:ExecutionContext){const req=ctx.switchToHttp().getRequest();const auth=req.headers.authorization;if(!auth?.startsWith('Bearer '))throw new UnauthorizedException('Internal token required');req.internalService=await this.tokens.validate(auth.slice(7));return true}}
