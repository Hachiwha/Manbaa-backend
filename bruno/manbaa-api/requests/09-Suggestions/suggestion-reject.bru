meta {
  name: Reject Suggestion
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1/workspaces/{{workspaceId}}/canvases/{{canvasId}}/suggestions/{{suggestionId}}/reject
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 201
}
