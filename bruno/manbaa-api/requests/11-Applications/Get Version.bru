meta {
  name: Get Version
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/applications/{{applicationId}}/versions/{{versionId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

assert {
  res.status: eq 200
}
