meta {
  name: Get Application
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/applications/{{applicationId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

assert {
  res.status: eq 200
  res.body.data.id: eq {{applicationId}}
}
