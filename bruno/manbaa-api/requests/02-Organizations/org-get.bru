meta {
  name: Get Organization
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/organizations/{{organizationId}}
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{organizationId}}
}
