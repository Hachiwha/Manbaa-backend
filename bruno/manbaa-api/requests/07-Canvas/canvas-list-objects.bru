meta {
  name: List Canvas Objects
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/workflows/{{workflowId}}/canvas/objects
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
