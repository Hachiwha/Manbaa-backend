meta {
  name: Override Session Status (Admin)
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/v1/sessions/{{sessionId}}/status
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "status": "active"
  }
}

assert {
  res.status: eq 200
}
