meta {
  name: Update Session Mode
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/v1/sessions/{{sessionId}}/mode
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "mode": "guided"
  }
}

assert {
  res.status: eq 200
}
