meta {
  name: Get Session
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/sessions/{{sessionId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

assert {
  res.status: eq 200
  res.body.data.id: eq {{sessionId}}
}
