meta {
  name: Get AI Task
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/workspaces/{{workspaceId}}/ai/tasks/{{taskId}}
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{taskId}}
}
